# Strategy B: Play Mode Integration Test SOP

> **Superseded (2026-07-18).** This SOP has been merged into the skill
> `.agents/skills/unity-card-playmode-test/SKILL.md`, which is now the **single source of truth**
> for Strategy B Play Mode integration tests. All content formerly maintained here — including the
> challenge table (rows on consecutive-trigger chain closing, `CheckCost_IndexBeforeStartCard`,
> async status-effect projectiles), the `ResetState` / `CloseChain` / `StartAnimationPhase` helpers,
> the async projectile workaround, and the GRUDGE example — now lives in the skill, alongside
> corrections verified by a live Play Mode run on 2026-07-18.
>
> Do not edit this file to update the workflow; update the skill instead.
