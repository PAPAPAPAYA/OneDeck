# 4.0 Card Desc Specification (v1, 2026-08-26)

Mirror of the Notion page "4.0 card desc 书写规范 v1" (OD workspace, under `OD`). The desc is both the
design document and the prefab configuration sheet: every keyword maps 1:1 to a GameEvent trigger,
an Effect method, or a target predicate. Chinese keywords below are data (the actual desc language);
this document is the engineering-side reference.

Source data: Notion `4.0 card database` (107 rows as of 2026-08-26). Engine status verified against
the codebase on 2026-08-26 (attack-attribute redesign, phase 3).

## Sentence Grammar

```
[trigger][：][cost,]effect-clause[；effect-clause]*
```

- Omitted trigger = 揭晓时 (OnMeRevealed), the most common case.
- Clauses execute in written order, separated by 「；」.
- A cost/gate clause leads, separated by 「，」: `放逐1友方信徒，埋葬2敌方非生物`
  (if the cost cannot be paid, later clauses do not run).
- Plain sequential effects with no gating also use 「；」.

## Trigger Vocabulary

| Keyword | Unity event | Engine status |
|---------|-------------|---------------|
| 揭晓时 (omittable) | OnMeRevealed | ✅ |
| 遗言： | OnMeBuried | ✅ |
| 被置顶： | OnMeStaged | ✅ |
| 被强化： | OnMeGainedAttack | ✅ |
| 友方被强化时： | OnFriendlyCardGainedAttack | ✅ |
| 敌方诅咒被强化时： | OnEnemyCurseCardGainedAttack | ✅ |
| 友方被埋葬时： | OnFriendlyCardBuried | ✅ |
| 有卡被埋葬时： | OnAnyCardBuried | ✅ |
| 友方被放逐时： | OnFriendlyCardExiled | ✅ |
| 敌方诅咒揭晓时： | OnEnemyCurseCardRevealed | ✅ |
| 友方攻击时： | OnAnyFriendlyCardAttacked | ✅ |
| 有卡攻击时： | OnAnyCardAttacked | ✅ |
| 回合开始： | BeforeRoundStart | ✅ |
| 洗牌后： | AfterShuffle | ✅ |
| 苏醒： | OnMeRevived | ❌ not implemented |
| 友方苏醒时： | (revive event family) | ❌ not implemented |
| 回合结束： | (round-end event) | ❌ not implemented |
| 被动： | placed after the start card on every shuffle; immovable; effects always on while condition holds (relic-style) | ❌ not implemented (direction ratified 2026-08-26) |
| 信徒揭晓时： | reveal event filtered by typeID | ❌ not implemented |

A trigger may carry a source predicate: `友方遗言卡被埋葬时：复活自身`.

## Effect Verbs

| Keyword | Unity implementation | Engine status |
|---------|----------------------|---------------|
| 攻击 / 攻击xN | `AttackEffect.Attack` / `AttackTimes` | ✅ |
| 强化N[target] | `AttackGiverEffect` (attack +N) | ✅ |
| 弱化N[target] | attack -N (this-round via `attackModThisRound`) | 🟡 needs mass-apply variant |
| 埋葬N[target] | `BuryEffect` | ✅ |
| 埋葬卡组顶N卡 | `BuryEffect.BuryNextXCards` | ✅ |
| 置顶N[target] | `StageEffect` | ✅ |
| 延后N[target] | `CardManipulationEffect.Delay*` | ✅ |
| 放逐N[target] | `ExileEffect` | ✅ |
| 生成N信徒 | `AddTempCard` (RIFT token) | ✅ |
| 复制自身 | `AddTempCard.AddSelfToMe` | ✅ |
| 攻击力翻倍 | `AttackGiverEffect` (double self) | ✅ |
| 攻击力=X | `AttackResolverSource` | 🟡 needs new sources |
| 复活N[target] | `ReviveEffect`: reuses Stage movement + grave-only selection (index < start card); grave → deck top; raises awaken events + revive counter | ❌ not implemented (direction ratified 2026-08-26) |
| 延迟复活N[target] | revive variant: grave → just before the start card (revealed last this round, same landing slot as the R2 bounce) | ❌ not implemented (direction ratified 2026-08-26) |
| 攻击次数+N | grant `extraAttackTimes` | ❌ not implemented |
| 回响X | wrapper over R2 life bounce | 🟡 |

## Revive & Awaken (ratified 2026-08-26)

- 复活 = grave → deck top. 延迟复活 = grave → just before the start card (with no other
  interference it is revealed last this round; same landing slot as the R2 bounce).
  延迟复活 is a revive variant, not a separate mechanism.
- 苏醒 = "when revived"; fired only by revive effects — never by Stage or bounce.
- Selection: only cards with `index < startCardIndex`; excludes passive cards (never
  revivable) and neutral cards; exiled and reveal-zone cards can never be revived;
  empty grave = fizzle.
- Bounced (回响/life) cards land above the start card, never enter the grave, and
  cannot be revived.
- On revive: raise onMeRevived (苏醒) + faction revive events + per-round revive counter.
- Prerequisite: remove or comment out the legacy Revive remnants (`StatusEffect.Revive`,
  `CheckCost_Revive`, related display strings) to avoid confusion with the new mechanic.
- 延迟复活 landing slot = the R2 bounce slot (queue tail, index = startCardIndex + 1) —
  the bounce positioning logic is directly reusable.
- Terminology: when the target is in the grave, always write 复活, never 置顶 — the
  believer token's "stage 1 friendly" is 复活1友方, and "stage the enemy curse" is
  复活敌方诅咒. 置顶 is reserved for pulling from the unrevealed deck.

## Passive Cards (ratified 2026-08-26)

- No separate zone: a passive is a normal card carrying an `isPassive` marker.
- Placement: on every shuffle (including combat start) it is placed after the start card
  (index < startCardIndex, grave side); it is never revealed, takes no reveal slot, and
  costs no fatigue.
- Immovable: bury / stage / delay / exile / revive and any other displacement effects
  fail on it (excluded from movement-effect selection pools).
- Always on: its effects are permanent listeners that fire whenever their condition is
  met (Linger-like, but with a fixed position and no way to leave).
- Still a card: it can be enhanced, counts toward grave counts (e.g. 墓地每有1卡), and
  can be read by other cards' predicates (e.g. 攻击力=友方最高攻击力). Value effects do
  not exclude passives — only movement effects do.
- Analogy: a roguelike relic, but carried as a card and participating in card
  interactions.

## Target Predicate Grammar

```
[count][faction][predicate]卡
```

- Faction: 友方 / 敌方; omitted = either faction.
- Predicate: 生物 (= any card with attack, incl. 0) / 非生物 / 信徒 / 诅咒 / 遗言 /
  normal / uncommon / rare / 攻击力最高 / 攻击力最低 / 攻击次数最多.
- Omitted predicate = random (ratified 2026-08-26: no player choice points in combat).
- `自身` = this card; `该卡` = the target of the previous clause.
- Engine support: tag / typeID / isMinion filters ✅; max-attack only on Stage 🟡;
  rarity and 生物 predicates ❌.

## Value Sources

- Fixed: `强化1友方`
- Self attack: `每有1攻击力，埋葬卡组顶1卡`
- Counter: `墓地每有1友方，埋葬1敌方` / `攻击力=本回合放逐的信徒数量`
- Reference: `攻击力=该卡攻击力`

## Hard Rules

1. Terminology unification: only 卡组顶 / 遗言 / 强化
   (banned: 牌堆顶, 牌组顶, 亡语, 增强).
2. Arabic numerals; multi-hit is `攻击x2` (half-width x); ATK=0 lives in the ATK column,
   not in the desc (no more `攻击（0）`).
3. The `被动：` prefix and the 被动 tag must both be present; a mismatch is a data error.
4. The desc carries mechanics only; rationale and tuning notes go to `side note`.
5. One card, one desc line, no line breaks.

## Database Field Rules

| Field | Rule |
|-------|------|
| CARD_TYPE_ID | English SNAKE_CASE, identical to the prefab file name; **required** — the only Unity anchor |
| ATK | Empty for non-attacking cards (not 0); 0 only for an attacking card with 0 attack |
| rarity | normal / uncommon / rare (maps to Unity Common/Uncommon/Rare) |
| 生物 | 生物 / 非生物; = any card with attack, incl. 0 (ratified 2026-08-26); invariant: 生物 ⟺ ATK non-empty |
| tag | Axis marker, kept in sync with desc keywords |
| Unity 配置状态 | 已配置 / 可直接配置 / 需小改 / 需新机制 |

## Rewrite Examples

| Before | After |
|--------|-------|
| 被动：信徒揭晓时：埋葬牌堆顶1卡 | 被动：信徒揭晓时：埋葬卡组顶1卡 |
| 让墓地1友方攻击；墓地无友方则埋葬1友方亡语 | 让墓地1友方攻击；墓地无友方则埋葬1友方遗言卡 |
| 被动；回合开始时，强化1最低攻击力友方 | 被动：回合开始：强化1攻击力最低友方 |
| 攻击（0）；攻击力翻倍 | 攻击；攻击力翻倍 (ATK=0 lives in the ATK column) |
| 友方遗言被埋葬：复活自身；攻击 | 友方遗言卡被埋葬时：复活自身；攻击 |
| 放逐1友方信徒：埋葬2敌方非生物 | 放逐1友方信徒，埋葬2敌方非生物 (cost uses 「，」) |

## Decisions (ratified 2026-08-26)

1. 生物 definition: any card with the attack attribute, including attack 0.
   Invariant: 生物 ⟺ ATK column is non-empty. (Engine side: a card with
   `printedAttack = 0` still counts as a creature — needs an explicit marker, since
   `HasAttackDisplay` is false at 0 with no resolver.)
2. 复活 added to the tag vocabulary; full sync — every desc containing 复活 plus
   every 信徒-tagged card now carries 复活 (47 cards total). (ID51 is a delay card —
   whether 延后 enters the tag vocabulary is TBD.)
3. ID43 / ID60: 被动 tag fixed by owner; the 被动 prefix and tag must stay paired.
4. ID68 confirmed: bury the lowest-attack friendly and enhance that same card
   (该卡 reference).
5. Unmarked targets are always random — no player choice points in combat.
6. CARD_TYPE_ID fully assigned + desc mass-normalized (2026-08-26): 106 cards named
   (13 reuse existing prefab names; passives carry a RELIC_ prefix); descs rewritten per
   this spec (terminology/punctuation/count padding only — no design or value changes).
