# Regression Checklist — OneDeck Visual Bugs

Every bug-fix PR / commit must **append or update at least one row** in this table.
If a row becomes obsolete (code refactored away), mark it `~~strikethrough~~` and add `(Obsolete YYYY-MM-DD)` rather than deleting it.

---

## Legend

| Symbol | Meaning |
|--------|---------|
| ✅ | Verified & stable |
| ⚠️ | Fixed but needs re-verification |
| ~~strikethrough~~ | Obsolete (code refactored away) |

---

## Deck Movement & Positioning

| # | Scenario | System / Effect | Fixed Date | Status | Verification |
|---|----------|-----------------|------------|--------|--------------|
| ~~1~~ | ~~Bury/Stage animation has no visible movement (distance-zero)~~ | ~~`BuryEffect`, `StageEffect`, `CombatUXManager`~~ | ~~2026-05-18~~ | ~~(Obsolete 2026-06-13)~~ | ~~Superseded by row 20: logic-phase sync removed; animation movement is now ensured by `ApplyAnimationResult` + `UpdateAllPhysicalCardTargets` in the animation phase.~~ |
| 2 | Bury-then-Stage reactive chain causes wrong animation target index | `BuryEffect`, `StageEffect`, `EffectChainManager` | 2026-05-15 | ✅ | **Card:** StoneShell buries RisingFlame (onMeBuried→StageSelf)<br>**Check:** Final deck position is correct |
| 5 | Bury/Stage inserts moved card before pending slot-in cards | `ApplyAnimationResult`, `BuryEffect`, `StageEffect` | 2026-05-24 | ✅ | **Card:** Chain AddTempCard then Bury/Stage<br>**Check:** Moved card lands after pending cards |
| 7 | Stage/Bury animation target offset when pending cards exist in deck | `CalculateAnimationPositionAtIndex`, `CombatUXManager`, `RecorderAnimationPlayer` | 2026-05-24 | ✅ | **Card:** sacrificial_spirit (creates pending JU_ON) then soldier_skeleton (StageSelf)<br>**Check:** Peak and slot-in positions match logical top index<br>同时验证 `RecorderAnimationPlayer` 使用 `actualPhysIndex` 而非 `correctedIndex` 作为动画目标索引（日志中 `actualPhysIndex == targetIndex`）。 |
| 23 | Arc midpoint z jumps to fixed -80 during Stage/Bury/Shuffle/Reveal-to-bottom | `CombatUXManager`, `showPos` | 2026-06-14 | ⚠️ | **Card:** Any Bury/Stage card (e.g. StoneShell, grave_punch, RisingFlame, BOOSTER) + Start Card shuffle + normal reveal-to-bottom<br>**Check:** Mid-arc card z should be midway between start z and target z, not `-80`. Cards remain visible throughout the arc. |
| 25 | ExileEffect still syncs physical deck in logic phase | `ExileEffect`, `RecorderAnimationPlayer`, `ApplyAnimationResult` | 2026-06-19 | ⚠️ | **Card:** Any Exile card (ExileSelf / ExileMyCards / ExileTheirCards)<br>**Check:** Exiled card pops up from current position, then plays destroy animation; remaining cards tween to correct positions after last destroy completes. Verify exiling the currently revealed card and chaining GiveStatusEffect → Exile on the same target. |
| 43 | Cascade deck layout replaces the linear fan; all deck positions follow the smooth curve | `DeckCascadeLayout`, `DeckPositionCalculator`, `CombatUXManager` | 2026-07-17 | ⚠️ | **Setup:** Set `CombatUXManager.deckLayoutMode = Cascade`.<br>**Check:** The deck renders as a smooth cascade arc: front card (deck top) largest at the `physicalCardDeckPos` anchor, front segment sweeping up-left with shrinking size/spacing, tail hooking back at minimum spacing. With `deckLayoutMode = Linear`, the legacy linear fan renders byte-for-byte. |
| 44 | Reveal entry and reveal-to-bottom follow the cascade curve | `CombatUXManager`, `CombatManager` | 2026-07-17 | ⚠️ | **Card:** Any card.<br>**Check:** The top card flies from the cascade front anchor to the reveal zone with no visual jump; clicking again arcs it to the cascade tail end (index 0) at the deepest tail scale (legacy linear bottom when `deckLayoutMode = Linear`). |
| 45 | Bury/Stage/Exile peak and slot-in positions land on the cascade curve at per-depth scale | `BuryEffect`, `StageEffect`, `ExileEffect`, `CombatUXManager` | 2026-07-17 | ⚠️ | **Card:** StoneShell (Bury), grave_punch (Bury), RisingFlame/BOOSTER (Stage), any Exile card.<br>**Check:** Pop-up peaks, arc paths and slot-in targets follow the cascade curve; the moved card's scale lands directly at its cascade depth scale (no uniform-scale frame and no post-landing "breathing"). |
| 46 | Reactive chain bury → stage intermediate states render on the cascade curve | `ApplyAnimationResult`, `RecorderAnimationPlayer`, `BuryEffect`, `StageEffect` | 2026-07-17 | ⚠️ | **Card:** StoneShell buries RisingFlame (onMeBuried→StageSelf).<br>**Check:** Each intermediate deck state during the chain follows the cascade curve; the first animation's result is preserved instead of being overwritten by the final deck state. |
| 47 | Shuffle animation re-layouts the whole deck on the cascade curve | `StartCardShuffleEffect`, `CombatUXManager` | 2026-07-17 | ⚠️ | **Setup:** Reach the Start Card shuffle (or an overtime round re-shuffle).<br>**Check:** After shuffling, every card tweens to its cascade position with its per-index depth scale (uniform scale only in Linear mode); no linear-fan or zero-distance frames. |
| 50 | Reveal-zone card counts as cascade front card; deck no longer re-layouts on reveal | `CombatUXManager`, `GetCascadeDeckCount`, `MoveRevealedCardToBottom`, peel focus | 2026-07-17 | ⚠️ | **Setup:** Toggle `CombatUXManager.revealCardCountsAsDeckFront` (cascade must be on).<br>**Check:** With the flag on, revealing a card moves only the revealed card to the reveal zone — the rest of the deck does NOT slide or reshape; the reveal card visually covers the front slot (hovering toward camera). Clicking again arcs the card to the cascade tail and the deck slides forward exactly one step. Bury/Stage/AddTempCard targets and peel focus (cascade front slot) stays consistent while the reveal zone is occupied. With the flag off, the legacy per-reveal re-layout returns byte-for-byte. |
| 56 | Shuffle flip-down happens on landing, exposing faces (and shuffle order) mid-flight | `CombatUXManager`, `PlayShuffleAnimationInternal` | 2026-07-24 | ⚠️ | **Setup:** Reach the Start Card shuffle (or an overtime round re-shuffle).<br>**Check:** Every non-Start card flips face-down around its arc midpoint (mid-flight, staggered per card) and lands already face-down; Start Card keeps its face. Post-shuffle layout, scales, and input unblock are unchanged. |
| 58 | Pop-up peak scale is relative to the card's current TargetScale instead of the uniform deck size | `CombatUXManager`, `GetPopUpPeakScale` | 2026-07-26 | ⚠️ | **Card:** Any Bury/Stage card sitting deep in the cascade tail (e.g. StoneShell buried from the tail, BOOSTER StageSelf), plus AddTempCard (RIFT_INSECT / BLACKSMITH).<br>**Check:** Pop-up peak scale = the card's current `TargetScale` × `popUpScaleMultiplier` (default 1.0 = no rescale at peak); deep tail cards stay proportionally small at the peak and slot back in at their cascade depth scale with no scale pop; reveal-zone cards pop up at reveal size unchanged; `popUpScaleMultiplier` > 1 still enlarges every card proportionally. |
| 72 | `deckLayoutMode` is the single combat deck layout selector; legacy toggles removed | `CombatUXManager` (`deckLayoutMode`, `BuildCascadeConfig`, `GetCascadeDeckCount`, `GetDeckScaleAtIndex`, `GetCascadeJitterScale`), `DeckPositionCalculator` | 2026-08-15 | ⚠️ | **Step:** Set `CombatUXManager.deckLayoutMode` to each of Linear / Cascade / ArcLoop / FloatStack and enter combat.<br>**Check:** Each mode renders its own layout (Linear = legacy fan byte-for-byte, Cascade = smooth curve, ArcLoop = elliptical loop, FloatStack = direct stack with floating reveal); `enableCascadeDeckLayout` / `enableArcLoopDeckLayout` no longer exist on the component; reveal-to-bottom, reveal-front counting, per-depth scales and jitter all follow the selected mode. |

| 73 | Start Card's driven big shadow never released on the shuffle path — ghost shadow lingered at the shadow home during/after the Start Card shuffle | `CombatUXManager` (`PlayStartCardShuffleAnimation`, Float Stack shadow drive) | 2026-08-15 | ⚠️ | **Setup:** Float Stack mode (`deckLayoutMode = FloatStack`); reveal cards until the Start Card is revealed, then trigger the shuffle.<br>**Check:** The Start Card's shadow fades out as the shuffle flight begins; no shadow lingers at the shadow home (slot 0) after the Start Card lands in the deck; the next revealed card drives its own shadow normally. |
| 74 | Start Card's big shadow always visible in the deck — the start card prefab wires no `cardFace`, so `BuildFlipRoot` early-returned and its big-shadow auto-wire never ran; `bigShadowRenderer` stayed null and neither suppression nor the reveal drive could see the shadow | `CardPhysObjScript` (Awake fallback resolves `bigShadowRenderer` by name), `StartCardParent.prefab` | 2026-08-15 | ⚠️ | **Setup:** Float Stack mode; enter combat with the Start Card in the deck.<br>**Check:** The Start Card's big shadow is hidden while it sits in the deck; on its reveal the shadow drives to slot 0 like any card; on the shuffle it fades out (row 73) and stays hidden in the deck afterwards; normal cards' shadow behavior unchanged. |
| 75 | Float Stack big shadow grows by `floatStackRevealScale` on every play-mode Inspector edit — the re-drive captured the card's live (settled reveal-pose) scale as the shadow's size base; large decks (globalScale < 1) also rendered the shadow smaller than the reveal card (extra `* globalScale`) | `CardPhysObjScript` (`DriveBigShadowToPose` target = `baseScale * _bigShadowHomeLocalScale`), `CombatUXManager` (`TryDriveRevealBigShadow` passes `GetRevealZoneScale()`) | 2026-08-16 | ⚠️ | **Setup:** Float Stack mode (`deckLayoutMode = FloatStack`); reveal a card, then in play mode drag `floatStackShadowOffset` / `floatStackRevealScale` / `floatStackStepY` while the card is revealed; also test with a large deck (globalScale < 1, e.g. `maxHeightPx / (stepY * deckCount)` < 1).<br>**Check:** The shadow only moves with the offset and always matches the reveal card size — never grows per edit, never smaller on large decks; the next reveal drives a fresh correct shadow; return-to-deck fade (row 73) unchanged. |
| 76 | Reveal-zone attacker stays shrunk (deck size) after its attack animation in Float Stack mode — the attack return path used the raw `physicalCardRevealSize` (1,1,1) instead of the layout-driven reveal scale (`deckSize * revealScale * globalScale`) | `AttackAnimationManager` (`PlayAttackAnimationCoroutine` reveal-zone return tween + finally target sync), `CombatUXManager` (`GetRevealZoneScale` now public) | 2026-08-16 | ⚠️ | **Setup:** Float Stack mode (`deckLayoutMode = FloatStack`); attack with a reveal-zone card (e.g. BLACKSMITH).<br>**Check:** After wind-up/charge/overshoot the card returns to the reveal-zone enlarged size (`deckSize * floatStackRevealScale * globalScale`) and stays there — no snap back to deck size on the next layout update; Cascade/Linear/ArcLoop attack returns unchanged (byte-for-byte `physicalCardRevealSize`); reveal-z clamp and deck-focus restore (row 51) unchanged. |
| 77 | After a shuffle, the Start Card (the only card that stays face-up) pops up on its own — during the shuffle flight face-up cards sweep under a stationary cursor and arm `_hoverPending` (OnMouseEnter's blocked-state queue), which resumes after the shuffle with no new cursor interaction; the shuffle path never reset hover state | `CardPhysObjScript` (`hoverSuspended` + `ResetAllCombatHovers`, OnMouseEnter/UpdatePendingHover gates, RuntimeInitialize static reset), `CombatUXManager` (`PlayStartCardShuffleAnimation` suspend/resume) | 2026-08-16 | ⚠️ | **Setup:** Any layout mode; keep the cursor parked over the deck while the Start Card shuffle (or overtime re-shuffle) plays, then don't touch the mouse.<br>**Check:** No card pops up after the shuffle without a fresh hover; the Start Card (face-up in the deck) only pops when the cursor actually enters it after the shuffle; normal hover, fast A→B hover (row 60), reveal-entry pending (row 29) and force-hide during animations unchanged; `[Shuffle] START/DONE` + `[Hover]` logs show no `PENDING` between the two markers. |
| 78 | Float Stack driven big shadow lost sync with the revealed card (reveal flip squash, attack wind-up/charge/overshoot/return, emphasize pulse, hover pop-up all played shadow-less) — the shadow was re-parented to the deck anchor and tweened once to a static slot-0 home pose | `BigShadowFollower` (per-frame follow), `CardPhysObjScript` (`DriveBigShadowToPose` / `RestoreBigShadowFromDrive`), `CombatUXManager` (`TryDriveRevealBigShadow` follow offset) | 2026-08-16 | ⚠️ | **Setup:** Float Stack mode (`deckLayoutMode = FloatStack`); reveal any card, then attack with it (e.g. BLACKSMITH).<br>**Check:** The shadow squashes in sync with the reveal flip; during the attack it tracks wind-up/charge/overshoot/return, scales 1.4x/0.85x and rotates with the card, and lands exactly back at the slot-0 home; emphasize pulse and hover pop-up keep the shadow glued; the rest pose is identical to the pre-follow static home; Cascade/Linear/ArcLoop unchanged. |
| 79 | Lift offset on the driven big shadow (attack = card lifted; emphasize pulse = small lift): shadow grows an anti-light offset with the attack wind-up / emphasize scale-up, holds through charge/overshoot, eases back across the return / scale-down; offset magnitude scales with the card's live scale ratio | `BigShadowFollower` (`SetLift`, `scaleK`), `AttackAnimationManager` (wind-up / return / finally hooks), `RecorderAnimationPlayer` (`PlayEmphasizeAnimation` hooks), `CombatUXManager` (`floatStackShadowFollowLiftDir` / `floatStackShadowFollowLiftDistance`) | 2026-08-16 | ⚠️ | **Setup:** Float Stack mode; attack with a reveal-zone card (e.g. BLACKSMITH), and separately trigger an on-reveal effect (emphasize pulse).<br>**Check:** Attack: the shadow offset grows along `floatStackShadowFollowLiftDir` during the wind-up, holds during the charge (shrinking proportionally with the 0.85x charge scale), eases to zero on the return, and the shadow lands exactly back at home. Emphasize: the offset grows and settles with the 1.2x pulse (smaller than the attack lift, via `scaleK`). z stays pinned at the deck front (never dips under stack cards); an interrupted attack leaves no stuck offset. |
| 80 | Orphaned invisible big shadow left under the deck anchor when a driven-shadow card was destroyed/exiled mid-reveal (the restore callback early-returned on the destroyed card, never re-parenting or destroying the re-parented shadow) | `BigShadowFollower` (self-destruct on null source), `CardPhysObjScript.RestoreBigShadowFromDrive` | 2026-08-16 | ⚠️ | **Setup:** Float Stack mode; reveal a card, then exile/destroy it while revealed (e.g. an Exile effect targeting the revealed card).<br>**Check:** No invisible `PhysicalCardBigShadow` GameObject remains parented under `physicalCardDeckPos` after the destroy; the next reveal drives a fresh shadow normally. |
| 81 | Batch-move cards (Bury/Stage/PopUp/SlotIn batch) converge on the shared arc apex at the same z — apex z = (start.z + target.z) / 2 ties exactly for order-reversing pairs, and card text children sit 0.02–0.06 in front of their face, so the back card's text bleeds through the front card's face | `RecorderAnimationPlayer` (batch handlers resolve targets up front, launch staggered), `CombatUXManager` (`apexZOffset` rank offset in `MoveCardWithAnimation` + `MoveCardToTopPopUpBatch`, new `batchMoveStagger` field), `CardMoveConfig` (`apexZOffset`/`startDelay`) | 2026-08-29 | ⚠️ | **Card:** Any multi-card bury/stage batch (e.g. BuryNextXCards, StageEffect batch stage).<br>**Check:** Mid-arc batch cards stay strictly z-ordered at the shared apex — no text bleed-through onto the front card; cards take off staggered (~0.06s apart, tunable via `CombatUXManager.batchMoveStagger`) and still land at their `actualPhysIndex` deck slots; each card completes exactly once (no stuck input block). |

## Card Adding & Pending Cards

| # | Scenario | System / Effect | Fixed Date | Status | Verification |
|---|----------|-----------------|------------|--------|--------------|
| 3 | Existing cards snap instantly when new card added + Bury/Stage | `CardPhysObjScript`, `AddPhysicalCardToDeck`, `CombatUXManager` | 2026-05-17 | ✅ | **Card:** RIFT_INSECT adds card then Bury triggers<br>**Check:** Existing cards tween visibly instead of snapping |
| 4 | Pending cards (RIFT/AddTempCard) have wrong pop-up peak / slot-in position | `AddTempCard`, `PopUp`, `SlotIn`, `CombatUXManager` | 2026-05-24 | ✅ | **Card:** RIFT_INSECT or BLACKSMITH<br>**Check:** Pop-up peak and slot-in target match logical deck index |
| 8 | Newly created curse card's projectile flies off-screen | `CurseEffect`, `MoveToPopUpPosition`, `CombatUXManager` | 2026-05-24 | ✅ | **Card:** Any curse card that enhances a type not present in deck (e.g. JU_ON)<br>**Check:** Projectile flies to visible deck peak, not off-screen |
| 32 | Overtime fatigue cards appear instantly without popup/slot-in animation | `CombatManager.AddFatigueCards`, `StartCardShuffleEffect`, `RecorderAnimationPlayer` | 2026-06-21 | ⚠️ | **Setup:** Set `overtimeRoundThreshold=1` and `fatigueAmount>=1`. Play combat past round 1.<br>**Check:** When the new round starts, fatigue cards fly from popup position into the deck before the Start Card shuffle animation plays. Cards do not snap instantly. |
| 48 | AddTempCard pending slot-in lands on the cascade curve | `AddTempCard`, `CombatUXManager` | 2026-07-17 | ⚠️ | **Card:** RIFT_INSECT or BLACKSMITH.<br>**Check:** Pop-up peak and slot-in target match the cascade position computed with the FULL deck count (pending cards included); slot-in scale equals the card's cascade depth scale. |

## Layout & Focus

| # | Scenario | System / Effect | Fixed Date | Status | Verification |
|---|----------|-----------------|------------|--------|--------------|
| 6 | Deck-move animations play in wrong peeled/focused layout | `RecorderAnimationPlayer`, `CombatUXManager` | 2026-05-18 | ✅ | **Step:** Click a card to focus deck, then reveal Bury/Stage card<br>**Check:** Animation uses normal (non-focused) layout |
| ~~36~~ | ~~Chained off-reveal attacks popup before focus transition~~ | ~~`RecorderAnimationPlayer`, `CombatUXManager`, `AttackAnimationManager`~~ | ~~2026-06-30~~ | ~~(Obsolete 2026-07-01)~~ | ~~Off-reveal Attack recorders no longer popup; superseded by row 37.~~ |
| ~~37~~ | ~~Off-reveal Attack cards skip popup, use peel-deck focus + emphasize~~ | ~~`RecorderAnimationPlayer`, `CombatUXManager`~~ | ~~2026-07-01~~ | ~~(Obsolete 2026-07-02)~~ | ~~Superseded by row 41: off-reveal Attack cards no longer play emphasize; peel-deck focus is now the sole source-card feedback before the attack animation.~~ |
| 41 | Off-reveal Attack cards skip popup and emphasize, use peel-deck focus only | `RecorderAnimationPlayer`, `CombatUXManager` | 2026-07-02 | ⚠️ | **Card:** BOOSTER (`StageSelf` → two `GOBLIN_CHARGE_TEAM` `OnMeStaged`) or any off-reveal attack<br>**Check:** Deck peels to the source card, then attack animation plays directly. No emphasize scale pulse and no popup peak/slot-in cycle for the attacker.<br>**Also check non-Attack off-reveal effect (e.g. StageSelf/Bury):** popup → emphasize → effect → slotin; verify there is **no** peel-deck focus transition. |
| 42 | Batch status effect animation helpers share `targetCards` list, causing projectile/target list to be cleared when `ShouldSkipRequestForSourceCard` mutates an earlier request | `StatusEffectGiverEffect`, `EffectScript`, `BuryEffect`, `RecorderAnimationPlayer` | 2026-07-02 | ⚠️ | **Card:** UNFINISHED_ROBOT (`GiveSelfStatusEffect`) or any card that gives a status effect to itself/batch targets (e.g. self-Power, GiveAllFriendlyStatusEffect, POWER_TRANSFER consume/transfer).<br>**Check:** `StatusEffectProjectile` request has valid targets and `[CombatUXManager] SpawnProjectile START/COMPLETE` logs appear; `PopUpBatch`/`SlotInBatch` no longer empty the shared list used by the projectile request. Bury/Stage/Transfer/Consume batch helpers also use independent list copies. |
| ~~49~~ | ~~Deck focus peel and popup centering derive from cascade positions~~ | ~~`CombatUXManager`, `AttackAnimationManager`, `RecorderAnimationPlayer`~~ | ~~2026-07-17~~ | ~~(Obsolete 2026-08-03)~~ | ~~Superseded by row 66: peel focus no longer aligns to `deckFocusTargetPos`; the focus card moves to the cascade front slot (`physicalCardDeckPos` anchor) via the focused-segment layout seam.~~ |
| 53 | Card back renders tiny (native sprite size) / effectively invisible when face-down | `CardPhysObjScript`, `BuildFlipRoot` | 2026-07-20 | ⚠️ | **Setup:** Enter combat; the whole deck spawns face-down.<br>**Check:** Every deck card shows a full card-size back (the runtime `CardBack` renderer copies `cardFace`'s `drawMode`/`size`/`sharedMaterial`, not just the sprite); reveal flips the card up to the correct face; Start Card shuffle lands correctly-sized backs on the whole deck. |
| 51 | Reveal-zone card occluded by the deck once deck count grows (deck front z crosses the fixed reveal z); first fix used a live TargetPosition scan that sampled transient mid-animation states and baked in a wrong z, so front z is now analytic + landing re-clamp; attack return path in `AttackAnimationManager` also read the raw reveal pos and was routed through the helper | `CombatUXManager`, `GetRevealZonePosition`, `ReClampRevealZoneTargetZ`, `UpdateAllPhysicalCardTargets`, `CardPhysObjScript`, `AttackAnimationManager` | 2026-07-18 | ⚠️ | **Step:** (a) Small deck (e.g. 10 cards): reveal a card — it must land exactly at the configured `physicalCardRevealPos` z (legacy behavior). (b) 30+ card deck or mid-combat growth (RIFT_INSECT / BLACKSMITH `AddTempCard`): reveal — card must stay `revealZoneZGap` (default `|zOffset|`) in front of the deck front card. (c) Round-start Start Card shuffle → re-reveal while deck cards are still settling: reveal card must end up in front of the front card, never at equal z. (d) Reveal-zone attacker (e.g. BLACKSMITH) with a deck large enough to overlap the reveal zone: the attack return flight and the post-attack resting position must stay in front of the deck front card; the whole attack (wind-up/charge/overshoot) stays in the attacker's z plane with no z drift mid-flight.<br>**Check:** No overlap between reveal-zone card and deck front in all four cases; reveal-entry input unblock works (no soft-lock); peel focus exit/restore and shuffle visuals unchanged. |

| 59 | Emphasize/popup feedback plays while deck is still peel-focused from a previous off-reveal Attack | `RecorderAnimationPlayer`, `CombatUXManager` | 2026-07-30 | ✅ | **Card:** Deck with an off-reveal attack card (e.g. SPIKE_SKELETON) chained before a reveal-zone effect card (e.g. GRAVE_PUNCH).<br>**Check:** The emphasize scale pulse (and off-reveal popup) plays only after the deck restores to its normal layout; console shows `Deck still focused before popup/emphasize; waiting for RestoreDeckFocusCoroutine` before `PlayEmphasizeAnimation START`. Consecutive off-reveal attacks still transition focus via `TransitionFocusCoroutine` with no restore-then-re-peel flicker. |

| 65 | Off-reveal attack card plays its attack animation face-down (card back visible) — peel-focus path never flipped the attacker; only `PopUpCard` did | `RecorderAnimationPlayer` (flip inserted after `FocusOnCardCoroutine`, before the `Attack` request), `CardPhysObjScript.SetFaceUp` | 2026-08-02 | ⚠️ | **Card:** Deck with a never-revealed off-reveal attack card (e.g. SPIKE_SKELETON).<br>**Check:** Peel completes, the attacker flips face-up fully, THEN the attack animation plays; the card stays face-up afterwards (never-cover rule) until shuffled. Consecutive attacks on the same card flip only once (idempotent via `isFaceUp`); staged/already-revealed attackers do not re-flip; reveal-zone attacks unchanged; row 41 behavior (no emphasize/popup for attackers) unchanged. |
| 66 | Peel deck focus via cascade front slot: focused-segment layout seam (`_focusSegmentCount`) replaces the `_deckFocusOffset` / `deckFocusTargetPos` alignment | `CombatUXManager` (`GetLayoutDeckCount`, `StartPeelCoroutine`, `TransitionFocusCoroutine`, `RestoreDeckFocusCoroutine`) | 2026-08-03 | ⚠️ | **Card:** BOOSTER or any off-reveal attack (e.g. SPIKE_SKELETON bury trigger) with cascade on.<br>**Check:** Cards in front of the focus card slide down off-screen from their current positions with stagger; the focus card lands exactly on the `physicalCardDeckPos` anchor at the maximum cascade scale with the segment behind it following the curve; chained focus switches (deeper and shallower) re-diff the peeled set correctly with no double slide-down on already-peeled cards; restore tweens every card back to the full cascade layout including scales; reveal-zone attacks and legacy linear mode (`deckLayoutMode = Linear`) unchanged. |
| 82 | Reveal-card return-to-bottom flight started while the peel-deck restore was still tweening — `ReleaseDeckFocus` fires the restore fire-and-forget and input unblocks the same frame, so the next click's return arc ran concurrently with the restore's raw `transform.DOMove` tweens (which `KillTweens` does not track), letting the restore's delayed reveal-card tween drag the card back to the reveal position after it had already landed at the deck bottom | `CombatUXManager` (`MoveRevealedCardToIndex` now defers only `MoveCardWithAnimation` until `_isDeckFocused` clears via `PlayRevealedCardReturnFlightWhenRestoreCompletes`; bookkeeping / target snapshot stay immediate) | 2026-08-30 | ⚠️ | **Card:** Deck with an off-reveal attack card (e.g. SPIKE_SKELETON); click again within the restore window (~0.2–0.8s) after the attack batch ends.<br>**Check:** The returned card waits face-down at the reveal position until the deck finishes restoring, then arcs once to the bottom slot — no bounce back to the reveal position, no concurrent flight over a still-peeled deck; restore completes exactly once (final `UpdateAllPhysicalCardTargets` included); non-focused reveals still arc immediately; life-bounce return (`MoveRevealedCardToIndex` queue-tail path) behaves the same. |

---

## UI & HUD

| # | Scenario | System / Effect | Fixed Date | Status | Verification |
|---|----------|-----------------|------------|--------|--------------|
| 52 | HP compare bar renders fully in the enemy color (Filled `Image` ignores `fillAmount` when `sprite` is null, full-quad red segment covers the player segment) | `CombatHPBarPresenter` | 2026-07-18 | ⚠️ | **Setup:** Enter combat at 20/20 HP.<br>**Check:** Bar shows a left player-color half and a right enemy-color half; uneven HP renders proportional segment widths. Attack hits tween the segments and play ghost/flash/shake on the damaged side only; heals flash the healed side only. |
| 53 | HP compare bar drop shadow renders incorrectly or duplicates | `CombatHPBarPresenter` | 2026-07-21 | ⚠️ | **Step:** Enter combat.<br>**Check:** A single `BarShadow` renders behind all six segments (sibling index 0), offset down-right as one whole-bar silhouette (not per-segment). Take a hit: the shadow shakes with `barRoot`, never detaches or lags; ghost/flash/low-HP pulse leave the shadow shape unchanged. Combat -> Shop -> Combat: still exactly one `BarShadow` child. Unwire `barShadow` (or delete the child) and re-enter Play Mode: warning logged, fallback shadow created at `Awake()`, bar fully functional. |
| 54 | HP bar outer corners must stay rounded at any fill level; the moving fill seam must stay a straight vertical edge | `GameScene.unity` (`HPBarRoot/BarClip`), `CombatHPBarPresenter` | 2026-07-21 | ⚠️ | **Setup:** Enter combat at any HP split.<br>**Check:** All four outer corners of the bar render rounded (same `9-Sliced` sprite as PlayerIcon) at full, partial and near-empty fill; the PlayerSeg/EnemySeg seam stays a hard vertical line while fill tweens; ghost/flash layers never spill past the rounded corners; `BarShadow` is rounded and offset down-right; bar shake/low-HP pulse keep the silhouette intact. Corner radius tuning: `BarClip` Image `Pixels Per Unit Multiplier`. |

| 55 | Player/Enemy icons stay visible outside the Combat phase | `CombatIconPresenter`, `GameScene.unity` (`PlayerIcon`, `EnemyIcon`) | 2026-07-22 | ⚠️ | **Step:** Play through Combat -> Shop -> Combat.<br>**Check:** Both icons are inactive during Shop/Result phases and reappear at their anchored top corners on entering Combat; Awake never leaves them visible before the first combat. |
| 57 | NullReferenceException when shop spawns empty-slot placeholders (`EmptyCardSpaceParent` wires `cardFace` but no ColorSOs; `BuildFlipRoot` hit `ownerCardColor.value`) | `CardPhysObjScript`, `BuildFlipRoot`, `ShopUXManager` | 2026-07-24 | ⚠️ | **Setup:** Enter the shop with fewer cards than `deckSize` (or sell a card) so empty slots spawn/respawn.<br>**Check:** No NRE in Console; empty slots render their normal placeholder face and purchase/sell flows work; combat flips (reveal, shuffle cover) are unaffected. |
| 69 | HUD colors single-sourced to `GameColorPalette` — HP numeric/bar components (`HPNumericDisplay`, `HPNumericDisplayHorizontal`, `CombatHPBarPresenter`) dropped serialized ColorSO fields and read `GameColorPalette.<Name>Color` statics; Edit Mode previews live-update via `ColorSO.Changed` / `GameColorPalette.Changed` | `ColorSO.cs`, `GameColorPalette.cs` + `GameColorPalette.asset` ("HP Bar / Numeric"), the three HUD components | 2026-08-15 | ⚠️ | **Step:** In Edit Mode, change `Black.asset` (wired as `hpNormal`) value in the Inspector; rewire `hpBarEnemy` to another asset; then enter Play Mode combat.<br>**Check:** Numeric displays and compare bar recolor instantly in the Scene/Game view without entering Play Mode (both for asset value edits and palette field rewiring, incl. undo); runtime HUD colors match the palette; `hpNormalPlayer` = Navy, `hpNormalEnemy` = GreyWhite 2, `hpBarEnemy` = OpponentCardColor 2; no console warnings (all HP fields wired); `GameColorPaletteWiringTests` passes. |
| 70 | Damage floater colors single-sourced to `GameColorPalette` — `DamageFloaterPresenter` dropped serialized `playerColor`/`enemyColor` and reads `FloaterPlayerColor`/`FloaterEnemyColor` statics; both palette fields point to `Navy.asset` (user decision) | `GameColorPalette.cs` + `GameColorPalette.asset` ("Damage Floater"), `DamageFloaterPresenter.cs` | 2026-08-15 | ⚠️ | **Step:** Enter combat and deal damage to both sides; use the `DamageFloaterPresenterEditor` preview; then change `Navy.asset` value in Edit Mode.<br>**Check:** Floaters on both sides render in the palette `floaterPlayer`/`floaterEnemy` color (Navy) — both at runtime and in the editor preview; changing `Navy.asset` recolor new floaters; no console warnings (both floater fields wired); `GameColorPaletteWiringTests` (floater cases) passes. |
| 71 | HP numeric display colors split per side — `hpNormal` replaced by `hpNormalPlayer` (Navy) / `hpNormalEnemy` (GreyWhite 2); both numeric components resolve by their `side` field | `GameColorPalette.asset` + `GameColorPalette.cs`, `HPNumericDisplay`, `HPNumericDisplayHorizontal` | 2026-08-15 | ⚠️ | **Step:** In Edit Mode change `Navy.asset` then `GreyWhite 2.asset` values; enter Play Mode combat.<br>**Check:** Player HP digits follow `hpNormalPlayer` (Navy), enemy HP digits follow `hpNormalEnemy` (GreyWhite 2) — preview updates live per side; the compare bar is unaffected (`hpBarPlayer` still GreyWhite 2, same asset, different semantic field); runtime colors match; `GameColorPaletteWiringTests` passes. |

## Cost Check Feedback

| # | Scenario | System / Effect | Fixed Date | Status | Verification |
|---|----------|-----------------|------------|--------|--------------|
| 9 | Cost check failure has no visual feedback on card | `CostResultPresenter`, `CardPhysObjScript` | 2026-06-07 | ✅ | **Card:** Any card with a cost condition (e.g. Mana cost when Mana is 0)<br>**Check:** Revealed card shakes left-right via EffectRecorder sequence |
| 10 | afterShuffle Stage animation is invisible (zero-distance tween) | `CombatManager`, `StartCardShuffleEffect`, `StageEffect` | 2026-06-08 | ⚠️ | **Card:** BOOSTER (afterShuffle→Stage) after Start Card shuffle<br>**Check:** Dummy cards do NOT silently tween before Stage animation; `MoveToTopPopUpBatch` arc animation is visible.<br>**Note:** Re-verify after 2026-06-13 sync-removal change; `StageEffect` no longer syncs in logic phase. |
| 11 | afterShuffle effect animations not played until next player click | `CombatManager`, `EffectChainManager`, `RecorderAnimationPlayer` | 2026-06-09 | ✅ | **Card:** BOOSTER (afterShuffle→Stage) after Start Card shuffle<br>**Check:** Stage animation plays **automatically** after auto-reveal completes, without waiting for player click; `isPlayingEffectAnimations` blocks Phase 2 during playback |
| 12 | BOOSTER overlaps deck cards because afterShuffle starts before reveal-zone movement finishes | `CombatManager`, `ICombatVisuals`, `CombatUXManager`, `CardPhysObjScript` | 2026-06-09 | ✅ | **Card:** BOOSTER (afterShuffle→Stage) after Start Card shuffle<br>**Check:** BOOSTER fully reaches reveal zone before its emphasize/Stage animation starts; no overlap with deck cards |
| 13 | `MoveCardToTopPopUpBatch` dead-locks due to N `RegisterAnimation` calls vs 1 `CompleteAnimation` | `CombatUXManager`, `AnimationStateTracker`, `RecorderAnimationPlayer` | 2026-06-09 | ✅ | **Card:** BOOSTER (afterShuffle→Stage) after Start Card shuffle<br>**Check:** `PlayRecorderAnimationsAndWait` does **not** wait 5s on timeout; `AnimationStateTracker` log shows `pending` returns to 0 after batch completes; `BlockInput`/`UnblockInput` are paired 1:1 |
| 14 | Bury animation lost when buried card triggers reactive effects that close the recorder chain | `BuryEffect`, `EffectChainManager`, `RecorderAnimationPlayer` | 2026-06-10 | ✅ | **Card:** grave_punch + slime + start card deck<br>**Check:** Reveal grave_punch (BuryNextXCards). Verify slime plays `PopUpBatch` + `MoveToBottomBatch` animation visibly; console shows `[BuryEffect] Capture request to recorder=chain#...` (not `null`) |
| 15 | GiveSelfStatusEffect missing projectile animation | `StatusEffectGiverEffect`, `RecorderAnimationPlayer` | 2026-06-10 | ⚠️ | **Card:** Any card with GiveSelfStatusEffect (e.g. self-Power)<br>**Check:** Card pops up, projectile flies from self to self, then slots back in; `StatusEffectChange` already captured by `ApplyStatusEffectCore` |
| 29 | GivePowerToCardThatGotPower missing projectile animation | `PowerReactionEffect`, `StatusEffectGiverEffect`, `RecorderAnimationPlayer` | 2026-06-20 | ⚠️ | **Card:** WEAPON_SPIRIT (onFriendlyCardGotPower → GivePowerToCardThatGotPower)<br>**Check:** Target card pops up, projectile flies from source to target, then slots back in; `StatusEffectChange` already captured by `ApplyStatusEffectCore` |
| 30 | PowerReactionEffect nested Power text commits incrementally per projectile | `PowerReactionEffect`, `RecorderAnimationPlayer`, `CardScript`, `AnimationRequest`, `EffectScript`, `ConsumeStatusEffect` | 2026-06-20 | ⚠️ | **Card:** SACRIFICIAL_SWORD + POWER_CRAVER + WEAPON_SPIRIT in deck.<br>**Check:** After SACRIFICIAL_SWORD's projectile lands, POWER_CRAVER's text shows 1 Power. After WEAPON_SPIRIT's reaction projectile lands, text updates to 2 Power. The reaction's Power is not visible before its projectile arrives. |
| 31 | AmplifyStatusEffectGain missing projectile animation | `StatusEffectAmplifierEffect`, `StatusEffectGiverEffect`, `RecorderAnimationPlayer` | 2026-06-21 | ⚠️ | **Card:** Any card with `StatusEffectAmplifierEffect` on `onMeGotStatusEffect` (e.g. self-Power amplifier).<br>**Check:** When the amplifier triggers, the card pops up, projectile flies in, then slots back in; `StatusEffectChange` already captured by `ApplyStatusEffectCore` inside `GiveSelfStatusEffect`. |
| 20 | JU_ON consumed by PREMATURE then Staged slots back to wrong position | `StageEffect`, `BuryEffect`, `RecorderAnimationPlayer`, `ApplyAnimationResult` | 2026-06-13 | ⚠️ | **Card:** PREMATURE + JU_ON in enemy deck<br>**Check:** PREMATURE reveals and consumes JU_ON's curse Power; JU_ON pops up from original deck position, projectile flies to `statusEffectConsumePos`, slots back to original index, then Stage arc moves it to deck top. No zero-distance SlotIn or misplaced landing. |
| ~~33~~ | ~~Off-reveal card activations have no visual cue before emphasize/shake~~ | ~~`RecorderAnimationPlayer`, `CombatUXManager`, `CardPhysObjScript`, `EffectRecorder`, `EffectChainManager`, `CostNEffectContainer`~~ | ~~2026-06-21~~ | ~~(Obsolete 2026-06-30)~~ | ~~Superseded by row 35: source card stays at popup peak during its effect animation.~~ |

## Status Effect Consumption Animation

| # | Scenario | System / Effect | Fixed Date | Status | Verification |
|---|----------|-----------------|------------|--------|--------------|
| 16 | ConsumeOwnStatusEffect missing projectile animation | `ConsumeStatusEffect`, `AnimationRequest`, `RecorderAnimationPlayer`, `CombatUXManager`, `ICombatVisuals` | 2026-06-12 | ⚠️ | **Card:** OVERCHARGED_SUMMONER (Power×1), DR_MANHATTAN (Power×4), ADVANCE_PORTAL (Counter×2), ALMIGHTY (Counter×2), SLIME (Counter×2)<br>**Check:** Revealed card pops up, projectile flies from card toward `statusEffectConsumePos`, status text updates after projectile lands, then card slots back in. No freeze or missing SlotIn. |
| 17 | Status effect projectiles do not reflect stack count | `AnimationRequest`, `ICombatVisuals`, `RecorderAnimationPlayer`, `CombatUXManager`, `StatusEffectGiverEffect`, `CurseEffect`, `ConsumeStatusEffect` | 2026-06-12 | ⚠️ | **Card:** Any card that gives/consumes multiple status effect layers (e.g. DR_MANHATTAN consumes 4 Power, a card giving Power×3 to all friendly cards)<br>**Check:** One projectile spawns per status effect layer; start positions are randomized within `projectileStartRandomOffsetRange`; launch times are staggered within `projectileStartTimeStaggerRange`; animation waits until the last projectile lands before SlotIn/StatusEffectChange commit. Total projectiles respect `maxProjectilesPerRequest`. |
| 18 | ConsumeRandomEnemyCardsStatusEffect has no projectile and queues per target | `ConsumeStatusEffect`, `EffectScript`, `RecorderAnimationPlayer`, `CombatUXManager` | 2026-06-13 | ⚠️ | **Card:** POWER_TRANSFER (Power×2) with multiple enemy Power cards in deck/reveal zone<br>**Check:** All target enemy cards pop up together; projectiles fly from each target back to the source card in parallel (absorb); status text updates after projectiles land; all targets slot back in together. No freeze or missing SlotIn. |
| 19 | ConsumeHostileCursePower has no PopUp/SlotIn/Projectile animation | `CurseEffect`, `EffectScript`, `RecorderAnimationPlayer`, `CombatUXManager`, `AnimationRequest`, `ICombatVisuals` | 2026-06-13 | ⚠️ | **Card:** CURSE_SUMMONER or PREMATURE when enemy curse cards carry Power<br>**Check:** All target enemy curse cards pop up together; projectiles fly from each target toward `statusEffectConsumePos` in parallel with one projectile per consumed layer; status text updates after projectiles land; all targets slot back in together. Source card does not PopUp separately. No freeze or missing SlotIn. |
| 21 | TransferAllStatusEffectToHostileCurse has no PopUp/SlotIn/Projectile animation | `TransferStatusEffectEffect`, `EffectScript`, `RecorderAnimationPlayer`, `CombatUXManager`, `AnimationRequest` | 2026-06-13 | ⚠️ | **Card:** CROW_CROWD when friendly/hostile cards carry Power and a hostile curse card is present<br>**Check:** Source cards pop up together; target curse card pops up; projectiles fly from each source to the target curse card **in parallel**; target curse status text commits only after the **last** projectile lands; source cards and target curse card slot back in together. No freeze or missing SlotIn. |
| 22 | TransferOneStatusEffectToSelf source cards do not PopUp/SlotIn | `TransferStatusEffectEffect`, `EffectScript`, `RecorderAnimationPlayer`, `CombatUXManager`, `AnimationRequest` | 2026-06-13 | ⚠️ | **Card:** POWER_SIPHONER when friendly/hostile cards carry the target status effect<br>**Check:** Source cards pop up together; self card pops up; projectiles fly from each source to self **in parallel**; self card status text commits only after the **last** projectile lands; source cards and self card slot back in together. No freeze or missing SlotIn. |
| 24 | Single-layer status effect projectile has random start offset | `CombatUXManager` | 2026-06-14 | ⚠️ | **Card:** Any card that gives/consumes exactly 1 status effect layer (e.g. self-Power×1, ConsumeOwnStatusEffect×1, GiveStatusEffect×1 to a single target)<br>**Check:** Projectile starts at the center of the source card (`effectiveOffsetRange == Vector2.zero`) and flies straight to the target; no visible XY jitter. Multi-layer effects still randomize. |
| 34 | Consume/transfer source cards update status text when projectile spawns | `RecorderAnimationPlayer`, `AnimationRequest`, `EffectScript`, `ConsumeStatusEffect`, `TransferStatusEffectEffect` | 2026-06-21 | ⚠️ | **Card:** OVERCHARGED_SUMMONER (ConsumeOwnStatusEffect), POWER_TRANSFER (ConsumeRandomEnemyCardsStatusEffect), CROW_CROWD/POWER_SIPHONER (TransferStatusEffectEffect)<br>**Check:** Source/target cards losing status effects show updated text the moment the projectile is spawned (flight starts). Cards receiving status effects still update only after the projectile lands. |
| ~~35~~ | ~~Off-reveal source card stays at popup peak during its effect animation~~ | ~~`RecorderAnimationPlayer`, `CombatUXManager`, `CardPhysObjScript`~~ | ~~2026-06-30~~ | ~~(Obsolete 2026-07-01)~~ | ~~Superseded by row 38: source-card slot-in is now deferred until the entire recorder subtree finishes.~~ |
| ~~38~~ | ~~Off-reveal source card slot-in deferred until recorder subtree finishes~~ | ~~`RecorderAnimationPlayer`~~ | ~~2026-07-01~~ | ~~(Obsolete 2026-07-01)~~ | ~~Superseded by row 39: slot-in now happens after the recorder's own requests finish, not after the subtree.~~ |
| ~~39~~ | ~~Off-reveal source card auto popup/slotin scoped to recorder's own requests~~ | ~~`RecorderAnimationPlayer`~~ | ~~2026-07-01~~ | ~~(Obsolete 2026-07-01)~~ | ~~Superseded by row 40: popup/slotin scope is now per source card across all recorders in a batch.~~ |
| 40 | Off-reveal source card auto popup/slotin scoped per card across recorders | `RecorderAnimationPlayer` | 2026-07-01 | ⚠️ | **Card:** Any card with multiple `CostNEffectContainer`s or reactive chains while off-reveal (e.g. multi-cost or onMeBuried→StageSelf).<br>**Check:** The source card pops up once before its first recorder's emphasize/shake, stays at peak across all recorders that share it, and slots in once after the last such recorder finishes. No bounce/popup-per-recorder. Built-in PopUp/SlotIn requests targeting the source card are skipped. Attack recorders reuse the existing popup or fall back to peel-deck focus. |

---

## Card Description & Dynamic Damage

| # | Scenario | System / Effect | Fixed Date | Status | Verification |
|---|----------|-----------------|------------|--------|--------------|
| 32 | Dynamic damage `<dmg>` placeholder shows as raw text during attack animation | `CardScript`, `RecorderAnimationPlayer` | 2026-06-21 | ⚠️ | **Card:** UNFINISHED_ROBOT (or any card with `<dmg>` in description that also receives a pending StatusEffectChange during its attack animation).<br>**Check:** During the attack animation, the card description shows the resolved number (e.g. `0 (+1)`) instead of `<dmg>`; Console shows no `[DynamicDamageDisplay] ... raw <dmg> ...` warnings. |
| 33 | Shop cards show raw `<dmg>` / `<dmg:key>` placeholders instead of damage numbers | `ShopUXManager`, `CardPhysObjScript`, `CardScript` | 2026-06-30 | ⚠️ | **Card:** Any card with `<dmg>` or `<dmg:key>` in description (e.g. GOBLIN_CHARGE_TEAM).<br>**Check:** Enter Shop phase and verify the card description shows numeric damage values instead of raw placeholders. |

---

## Quick Search

Before editing any code in `Effects/`, `UXPrototype/`, or `Managers/Animation*.cs`:

1. Grep for `VISUAL-FIX` in the files you modified
2. Find the matching scenario number above
3. Confirm you did not break the **Regress** scenario

### Search by File

| File(s) | Related Rows |
|---------|-------------|
| `BuryEffect.cs` | 1, 2, 5, 7, 14, 42, 45, 46 |
| `StageEffect.cs` | 1, 2, 5, 7, 45, 46 |
| `CombatUXManager.cs` | 1, 3, 4, 6, 7, 12, 13, 17, 19, 35, 43, 44, 45, 47, 48, 49, 51, 56, 58, 60, 61, 75, 77 |
| `CombatHPBarPresenter.cs` | 52, 53, 54 |
| `DamageFloaterPresenter.cs` | 30, 67 |
| `CombatInfoDisplayer.cs` | 67, 68 |
| `HPAlterEffect.cs` | 67, 68 |
| `GameScene.unity` (`HPBarRoot`) | 54 |
| `EffectChainManager.cs` | 2, 11 |
| `CardPhysObjScript.cs` | 3, 12, 35, 51, 53, 57, 60, 61, 62, 63, 75, 77 |
| `StartCardParent.prefab` | 62 |
| `CurseEffect.cs` | 8, 17, 19 |
| `AddTempCard.cs` | 4, 48 |
| `RecorderAnimationPlayer.cs` | 6, 9, 11, 13, 17, 19, 33, 35, 38, 40, 41, 42, 46, 49 |
| `AttackAnimationManager.cs` | 51, 76 |
| `ApplyAnimationResult` | 5, 46 |
| `CalculateAnimationPositionAtIndex` | 7, 44, 45, 48 |
| `DeckCascadeLayout.cs` | 43 |
| `DeckPositionCalculator.cs` | 43 |
| `CostResultPresenter.cs` | 9 |
| `CostNEffectContainer.cs` | 9, 33 |
| `EffectRecorder.cs` | 33 |
| `AnimationRequest.cs` | 9, 16, 17, 19, 30 |
| `CardScript.cs` | 30, 32 |
| `CombatManager.cs` | 10, 11, 12, 44 |
| `ConsumeStatusEffect.cs` | 16, 17, 18, 30 |
| `ICombatVisuals.cs` | 16, 17, 19 |
| `NullCombatVisuals.cs` | 16, 17, 19 |
| `NullCombatVisualsBehaviour.cs` | 16, 17, 19 |
| `StatusEffectGiverEffect.cs` | 17, 42 |
| `PowerReactionEffect.cs` | 30 |
| `EffectScript.cs` | 18, 19, 30, 42 |

## Lifecycle & Destroy Guards

| # | Scenario | System / Effect | Fixed Date | Status | Verification |
|---|----------|-----------------|------------|--------|--------------|
| 26 | `MissingReferenceException` when exiting combat while effect animations are still playing | `CombatManager`, `PhaseManager`, `EffectChainManager`, `RecorderAnimationPlayer` | 2026-06-19 | ⚠️ | **Step:** Trigger a combat-ending effect (e.g. last revealed card kills enemy) while `isPlayingEffectAnimations == true`, or spam-click/auto-reveal through combat end.<br>**Check:** PhaseManager waits for `isPlayingEffectAnimations == false` before calling `ExitCombat()`; `HandleCombatFinished()` does not set `combatFinished` until animations complete; `CloseOpenedChain()` and `RecorderAnimationPlayer` skip destroyed cards/recorders instead of crashing; console shows no `MissingReferenceException` from `PlayRecorderAnimationsAndWait`. |
| 27 | Combat-scoped value trackers leak stale values into the shop phase | `PhaseManager`, `ValueTrackerManager` | 2026-06-19 | ⚠️ | **Step:** Finish a combat where trackers such as `totalPowerCountInDeckRef`, `ownerCardsBuriedCountRef`, `stagedOwnerRef`, or `lastAppliedStatusEffectAmountRef` are non-zero, then enter the shop phase.<br>**Check:** After `EnteringShopPhase()` runs, all `ValueTrackerManager` IntSOs are reset to their original values and `lastAppliedStatusEffectRef.value` is restored to `valueOg`; no stale combat values affect shop/result logic. |
| 28 | Exiled reveal-zone card causes next card to be triggered mid-flight when autoReveal is on | `CombatUXManager`, `CombatManager`, `RevealCards` | 2026-06-19 | ⚠️ | **Card:** Any Exile card that exiles the currently revealed card (e.g. ExileSelf) with `autoReveal = true`.<br>**Check:** After the exiled card's Destroy animation completes, the next card auto-reveals and flies to reveal zone; input and auto-trigger are blocked until the card fully reaches reveal zone. Verify normal manual reveal and Round Start Start Card shuffle still behave correctly. |
| 29 | Hover card tag tooltip: cascade overlap arbitration, combat pop-up, autoReveal pause | `CardPhysObjScript`, `CardTagTooltip` | 2026-07-23 | ⚠️ | **Step:** In Combat, hover a face-up deck card deep in the cascade tail, then sweep across overlapping cards; hover the reveal-zone card; enable `autoReveal` and hover a card; trigger an effect animation while hovering; hover a face-down deck card; hover a shop card.<br>**Check:** Only the frontmost (smallest z) card reacts; face-up deck card pops up immediately and `autoReveal` pauses/restores on exit; reveal-zone card shows tooltip without pop-up; tooltip appears after `hoverDelay` and anchors to the card (see row 31); hover is fully disabled during `isPlayingEffectAnimations`/input block and cleans up if interrupted; face-down cards never show tag info; shop cards show tooltip only (no pop-up, no autoReveal change). |
| 30 | Damage floaters spawn off-screen at the top corners | `DamageFloaterPresenter` | 2026-07-26 | ⚠️ | **Step:** In combat, deal damage to the player and to the enemy (both `AttackAnimationManager` targets are near/outside the camera frustum edges).<br>**Check:** Each floater is clamped inside `FloaterLayer` (see VISUAL-FIX(2026-07-26) in `DamageFloaterPresenter.ClampToLayer`) and stays fully visible for its whole lifetime — punch, hold, and the 62px float-up fade; both sides visible; burst hits readable with x jitter. |
| 31 | Tag tooltip shows per-tag explanation lines and anchors to the card instead of the mouse | `CardTagTooltip`, `TagTooltipDatabaseSO` | 2026-07-26 | ⚠️ | **Step:** In Combat, hover a face-up card that has a tag with a configured description (e.g. Linger/DeathRattle), a tag without one, and multiple tags at once; hover a card near the right screen edge (e.g. reveal zone); repeat in the Shop.<br>**Check:** Each visible tag shows a bold `[Tag]` title line with its explanation (from `TagTooltipDatabaseSO`) on the next line, tag blocks separated by a blank line (tags without a configured StringSO show the title only); the panel sits at the card's right edge by default and flips to the left edge when it would overflow the screen; vertically centered on the card and clamped inside the screen; the panel tracks the card during pop-up/slot-in tweens; `TagDesc_*` StringSO assets keep their text (reset = false). |
| 60 | Fast hover A→B while A's pop-up is still playing: B never pops up until cursor re-entry; A's pop-up tween fights its slot-in tween and its input block outlives the hover | `CardPhysObjScript` (`_hoverPending`, `UpdatePendingHover`, `IsInputBlockedByNonPopUp`), `CombatUXManager` (`InterruptActivePopUpSlotIn`, `PopUpCard`, `SlotInCard`, `PopUpSlotInInputBlockCount`), `CombatManager` (`InputBlockCount`) | 2026-07-31 | ⚠️ | **Step:** In Combat, hover a face-up deck card A; while its pop-up is still playing, move the cursor to another face-up card B; also try A→B→off-deck inside the slot-in window, sweeping across cascade-overlapped cards, and hovering during reveal-entry/shuffle/effect animations.<br>**Check:** A slots back from its mid-air position in one clean motion (no tween fight/jump); B pops up **immediately** while A is still slotting back (parallel), without moving the cursor off B; B never pops if the cursor leaves B before the gates open; the frontmost card still wins arbitration; hover stays blocked during reveal-entry, reveal-to-bottom, shuffle and effect animations; afterwards `IsInputBlocked == false` (click-to-reveal works) and `AnimationStateTracker` pending returns to 0; face-down cards still never react (the Start Card now reacts and pops up — see row 62); off-reveal recorder popup→slot-in flows (e.g. onMeBuried→StageSelf) complete with no hang; Shop hover tooltip unchanged. |
| 61 | Already-popped-up card pops up again mid-flight and climbs without bound (stair-stepping cards) | `CombatUXManager` (`PopUpCard` peak anchored to deck slot, `popUpOriginal*` preserved on interrupt), `CardPhysObjScript` (`OnMouseEnter` `_hoverActive` guard) | 2026-07-31 | ⚠️ | **Step:** In Combat, wiggle the cursor on/off the same face-up deck card rapidly (re-pop-up mid pop-up and mid slot-in); hold the cursor still while other deck cards move across it (collider re-cross); sweep A→B→A quickly.<br>**Check:** The card always rises to the same peak above its logical deck slot, never higher; no visible popup restart glitch while the cursor stays on the same card; slot-in always returns the card to its deck slot; reveal-zone card pop-up behavior unchanged; afterwards `IsInputBlocked == false`. |
| 62 | Hovering the Start Card popped up the cards behind it, then the Start Card itself would not pop up (Start Card missing from hover occlusion: prefab had no `BoxCollider2D`; hover gates classified it as non-hoverable; prefab `currentGamePhaseRef` unwired so `IsInCombatPhase()` was false and `BeginHover` took the tooltip-only shop path) | `StartCardParent.prefab` (added `BoxCollider2D`, wired `currentGamePhaseRef`), `CardPhysObjScript` (`OnMouseEnter`, `UpdatePendingHover`, `BeginHover`) | 2026-08-02 | ⚠️ | **Step:** In Combat, hover the Start Card (in the deck after the shuffle, and in the reveal zone before it); move the cursor from the Start Card onto a behind card's visible area; hover→leave→hover repeatedly; sweep fast from a normal card onto the Start Card and back.<br>**Check:** The Start Card pops up itself and cards behind it stay slotted (frontmost z wins arbitration); a behind card pops up only once the cursor is actually on it; after slot-in the Start Card stays face-up (never-cover rule); fast A→Start Card transfers behave like normal A→B (row 60); Shop phase hover unchanged. |
| 63 | Start Card silently became face-down after hover slot-in / deck moves, then hover pop-up stopped working (it spawns face-up without a flip, so `everRevealed` was never set and the never-cover guard did not protect it) | `CardPhysObjScript` (`SetFaceUp` never-cover guard now also skips `isPhysicalStartCard`) | 2026-08-02 | ⚠️ | **Step:** In Combat, hover-pop the Start Card and let it slot back in; also move it via Bury/Stage/Delay and reveal-to-bottom; repeat hover after each move.<br>**Check:** The Start Card stays face-up after every move/slot-in and remains hover-poppable (`isFaceUp` never flips false); other cards still cover normally (fresh deck cards face-down, ever-revealed cards stay up, shuffle force-cover unchanged); Start Card keeps its face through the round-start shuffle. |
| 64 | Reveal-zone card z stale while generated cards enter the deck — the deck front card slides one cascade step toward the camera on spawn, but the reveal card was only re-clamped at the end of `PlayRecorderAnimationsAndWait`, so it could sit behind the deck front for the whole animation phase | `CombatUXManager` (`AddPhysicalCardToDeck` now calls `TryReClampRevealZoneTargetZ`, a helper shared with `UpdateAllPhysicalCardTargets`) | 2026-08-02 | ⚠️ | **Step:** In Combat with a card in the reveal zone and a near-capacity deck, play a card that generates temp cards (e.g. RIFT_INSECT / BLACKSMITH).<br>**Check:** During the generator's PopUp/SlotIn animation (not only after it), the reveal-zone card stays in front of the deck front card with no z-overlap flicker; reveal-entry tween and input unblock still work (2026-07-18 reveal-z fix); existing deck cards still animate visibly on subsequent Bury/Stage (2026-05-17 fix). |
| 67 | Damage floater numbers swapped between hits — a reactive chain (bury → onMeBuried damage) resolves its damage between the parent's two logic hits but plays its attack animation after them, and the old absolute-value FIFO popped snapshots in playback order, attaching each number to the wrong hit | `CombatInfoDisplayer` (delta commit: `SnapshotHpDisplay` freeze count + `CommitHpDisplay(target, hpLoss)` + `onHpDisplayCommitted`), `HPAlterEffect` (captures `actualHpLoss` per hit), `DamageFloaterPresenter` (event-driven; frame polling only while no pending commits) | 2026-08-04 | ✅ | **Card:** SMALL_SCALE_DEATH (尸爆: 2x2 damage, buries ETERNAL_GHOST mid-effect) + ETERNAL_GHOST (1 damage onMeBuried).<br>**Check:** Floaters read 2 (reveal hit), 2 (尸爆 attack anim), 1 (ghost attack anim) — not the swapped 2/1/2; fully shield-absorbed hits spawn no floater; status-effect damage still floats via the fallback poll after the display unfreezes; console `[DamageFloater] Capture/Snapshot/Commit` logs pair per hit; `CombatInfoDisplayerDeltaCommitTests` covers the out-of-order commit case. |
| 68 | HP display jumped at reveal time (before any attack animation), then every commit showed a wrong value until the pending count drained and the getter fell back to live HP (a final "+1 correction") — a nested reactive hit (Linger on `onTheirPlayerTookDmg`) resolves INSIDE the outer hit's `CheckDmgTargets` event raise, and its mid-burst preHitHp won the batch freeze | `HPAlterEffect` (snapshot + attack capture moved BEFORE `CheckDmgTargets` event raise), `CombatInfoDisplayer` | 2026-08-04 | ✅ | **Card:** SMALL_SCALE_DEATH (尸爆) + ETERNAL_GHOST (不散的恶灵, Linger: 1 damage when the enemy takes damage).<br>**Check:** Enemy HP text stays at 25 until the first attack animation lands, then steps 23 → 21 → 20 with no jump at reveal and no final fallback correction; floaters stay 2/2/1 (row 67); `CombatInfoDisplayerDeltaCommitTests.NestedReactiveHit_OuterHitWinsTheFreeze` covers it. |
| 65 | Result stats panel header row stretched to fill half the panel (fake "row spacing too wide" between title, column header, and data rows) — inner `HorizontalLayoutGroup.childForceExpandHeight=true` leaked `flexibleHeight=1` through the row's `LayoutElement` (-1 falls through), so the half `VerticalLayoutGroup` split leftover height equally between Header and ScrollView | `ResultStatsPanel` (`BuildHalf` sets explicit `flexibleHeight = 0` on Title/Header/EmptyRow/Row LayoutElements) | 2026-08-02 | ⚠️ | **Step:** Enter the Result phase after any combat; tune `PhaseManager.resultStatsPanelLayout.headerRowHeight`.<br>**Check:** Title and column-header rows are exactly `headerRowHeight` tall and stack flush under each other; data rows start directly under the header (only `rowSpacing` apart); ScrollView absorbs all leftover height; `rowHeight` still controls data-row height independently. |
| 69 | Runtime HP digit strips render ~11px lower than the Edit Mode preview (mask slot is 1em tall with the strip line top-aligned, but the font's glyph block is 1.172em, so TMP Center alignment and the top-anchored strip line land the digits (glyphBlockEm - 1) * lineHeight / 2 apart) | `HPNumericDisplayHorizontal` (`CreateStrip` slot size/offset, `RepositionStrips` `slotOffsetY`, `ProbeStripMetrics`) | 2026-08-15 | ⚠️ | **Step:** In Edit Mode note the preview digit vertical center of `PlayerHPDisplayH` (previewHp=12/20); enter Combat and compare the runtime digits' vertical center; also trigger digit-count growth (99→100) and hits (shake/landing pop).<br>**Check:** Runtime current and max digits sit at the same vertical center as the Edit Mode preview (within ~1px); slash stays centered; digit-growth glide moves x only; low-HP pulse, hit shake and landing pop unchanged. |

| 70 | Physical card colors were driven by serialized ColorSO refs duplicated across the card template prefabs (not the palette): editing GameColorPalette had no effect on cards; opponent card color slot drifted to a different asset than the prefabs (uncommitted) | `CardPhysObjScript` (serialized ColorSO fields removed; reads `GameColorPalette` statics), `GameColorPalette` (new startCardColor/startCardTextColor slots + 8 statics, edge slots removed), template prefabs (`PhysicalCardParent`/`StartCardParent`/`MinionPhysicalCardParent`/`EmptyCardSpace`/`EmptyCardSpaceParent` color fields removed; `StartCardParent` face/name/desc wired; `EmptyCardSpaceParent` cardFace unwired), `ResultStatsPanel` (FactionColor statics), `GameColorPaletteWiringTests` (Card group goldens) | 2026-08-17 | ⚠️ | **Step:** Enter the Shop and Combat; check player card face/back (GreyWhite), enemy card face/back (Red 1 - now brighter), card text colors, the Start Card (gold face, dark text, stays face-up through moves/shuffle), shop empty slots (placeholder face, no flip machinery), Infected/Power tints; open the Result panel after a combat (YOU/ENEMY titles follow card colors); tune a color in `GameColorPalette.asset` and confirm cards follow immediately in Play Mode.<br>**Check:** Only the enemy card color changed vs before (Red 1 vs the old OpponentCardColor); everything else renders identically; no console NREs from `CardPhysObjScript`; `GameColorPaletteWiringTests` all green (incl. `HpFields_WiredToCanonicalAssets` after hpNormalEnemy → GreyWhite 2). |

| 71 | Remaining hardcoded overlay colors were not palette-driven: damage floater digit shadow, tag tooltip bg/text, result stats panel bg/text | `GameColorPalette` (new floaterShadow + Overlay Panels group slots/statics), `DamageFloaterPresenter` (shadow.effectColor), `CardTagTooltip` (bg/text), `ResultStatsPanel` (bg RGB + all white texts; alpha still from `ResultStatsPanelLayout.backgroundAlpha`), `GameColorPaletteWiringTests` (floaterShadow + overlay goldens; floaterEnemy moved to GreyWhite 2026-08-17) | 2026-08-17 | ⚠️ | **Step:** Deal damage to both sides (floater shadow behind digits); hover a card with a tag (tooltip black bg + white text); finish a combat and open the Result panel (black bg, white name/cell texts, grey "No damage recorded." half); tune the new palette slots and re-check.<br>**Check:** All render exactly as before (shadow 0.6 alpha, tooltip 0.85 bg, result panel alpha from layout); changing `floaterShadow`/`tooltipBg`/`tooltipText`/`resultPanelBg`/`resultPanelText` in `GameColorPalette.asset` updates them at runtime; result panel alpha still follows `resultStatsPanelLayout.backgroundAlpha`; `GameColorPaletteWiringTests` all green. |
