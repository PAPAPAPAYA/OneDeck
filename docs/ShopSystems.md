# Shop Systems

(Moved from AGENTS.md on 2026-09-05 during the size diet; that file keeps a condensed summary.)

## Duplicate Slot Rule

`ShopManager.duplicateCopiesShareSlotRef` (BoolSO in `Assets/SORefs/ShopRefs/`, default OFF). ON: copies sharing a `cardTypeID` take 1 deck slot (first copy only), stack upper-left in the shop display, and only the stack base shows price (`ShopCardView.suppressPriceDisplay`); count via `UtilityFuncManagerScript.CountCardsTakingUpSpace`; empty `cardTypeID` never deduped; `CombatStartCardGiver` and enemy deck unaffected. Shop empty slots are persistent background objects (`ShopUXManager._spawnedEmptySlots`, one per deckSize grid slot) — never consumed/respawned by buy/sell; `_spawnedPlayerCards` holds real cards only; buy/sell just add/remove + `RelayoutPlayerDeckCards()`. Plan: `plans/plan-duplicate-cards-share-deck-slot-2026-07-31.md`.

## Shop Utility Passives & Board Pipeline

Plan & execution state: `plans/plan-utility-passive-shop-pipeline-2026-08-31.md` (§6); probability + mixed-pool rework 2026-09-11: `plans/plan-shop-utility-probability-mixedpool-2026-09-11.md` (§5).

- Metadata: `CardScript.utilityKind` (+ `utilityValue`/`utilityValue2`/`utilityRarityWeightMults`/`reservedTag`); `IsUtilityPassive` = isPassive && kind != None. `utilityValue2` carries the per-board roll chance % for RaritySlot / ReservedTag / Odds / RerollDiscount kinds (≤0 → default 25 via `UtilityShopBonus.ChanceOrPercent`; cadence semantics retired 2026-09-11). ShopOptionChance (2026-09-14): `utilityValue` = slots added on a hit, `utilityValue2` = per-board chance % (≤0 → 25); `bonus.extraBoardSlots`/`extraBoardSlotsChancePercent` grow the generic slot count on a per-board hit, both modes, both board types (card: UTILITY_OPTION_P_1 魇市游摊; unconditional sibling OPTION_1 魇市新摊 moved to Uncommon same day). Passive utility = deck-resident, slot-free (`occupiesDeckSlot` = false, 2026-09-14: buy-capacity check skipped) but physical (`physicalDeckCard`, renamed from `takeUpSpace`: deck-band display, sellable, combat-instantiated per 2026-09-14 ruling A), sellable (sell removes it); all bonuses are RECOMPUTED from the deck — no accumulating listeners.
- `UtilityShopBonus` (pure static) derives all from `playerDeckRef`: baseline growth (payday/hpMax/deckSize step growth = `growthPerStep * floor(session / sessionsPerStep)`, interval 1 = legacy every-session slope), per-kind effects, owned dedup. Sole stateful exception: deck-slot meter card — `DeckSizeIncreaseEffect` bumps run counter `DeckSlotPurchasesRef` (reset in `PhaseManager.ResetRun`), self-exiles on buy (BuyFunc skips deck add), escalating price, capped by static `maxDeckSize` (16).
- Shop HP invariant: `ApplyHpMaxFromDeck` (entry/buy/sell) sets `hp = hpMax`; shop phase is always full HP, so combat starts full (`HPMaxAlterEffect` removed 2026-09-04).
- `ShopBoardPipeline.GenerateBoard` (pure static, injected System.Random): pool classification (owned-utility dedup + deck-size ceiling exclusion baked in) → MIXED mode (default, `ShopManager.splitUtilityCombatBoards = false`): `MergePools` (OddsUtility reference-dedup) then wave filters on the merged pool, no board-type roll; SPLIT mode (legacy): board-type roll (`sessionUtilityBoardChances`; empty utility pool → combat board) → wave on the chosen pool only. Both: weighted generic rolls (session table × shopRollWeightMultiplier × RarityWeight mults) → reserved slots appended last, never displacing generic slots (per-board independent chance roll, no cadence/pity; `firstBoardOnly` = ODDS deep form, board 0 only, skips the roll; candidates: mixed = merged pool, split = classified pool, utility promises (`wantsUtilityCard`) always from the utility pool; utility-board drought → any-rarity fallback, otherwise skip on empty). `CurrentBoardIsUtility` is only meaningful in split mode (drives the 奇物架 UX marker).
- Board discounts (`ShopManager.ApplyBoardDiscount`, called from EnterShop AND Reroll): every discount spec rolls its chance once per generated board (initial board included); on a hit ONE random board card gets `UtilityShopBonus.GoldOffForPercent` (half price rounds UP), struck-through display via `GetBoardDiscount`, settlement via `GetEffectiveBuyPrice`; the per-board dict is cleared on every generation (no accumulation across boards).
- Stats: result rows and `CountGraveyardCardsOf` exclude `IsUtilityPassive` (latter: no enemy GRAVE_CURSE feed); deck-population axis still counts passives; ShopStatsManager logs utility appear/buy share.

## Shop Top Bar & Page Layout Config (2026-09-23)

Lineage: row 0 = 离开商店 | avatar+username | HP pill | money | ❚❚, rows 1-2 = rarity odds / wins-hearts-income (2026-09-19 §08). 2026-09-21 the whole bar became PAGE content: `ShopChromeAnchor` deleted, root + `BandBottomWorldY` written once at Build, wheel scroll takes the bar away; a settled shop hides the canvas avatar/HP and shows the `ShopPageHud` world mirror (sprites/colors/fonts copied with `CanvasPxToWorld` calibration; handoff rule `inCombat || inResult || (inShop && IsTransitioning)`; the hidden HP pill parks at the shop anchor; the world pill plays a ~0.3 s count-up on hpMax changes).

2026-09-23 config, step 1 (cf8c52c, `plans/plan-shop-layout-config-widget-factory-2026-09-23.md`): every top-bar/mirror placement value moved from hardcoded consts into `ShopLayoutConfigSO` (`Assets/Resources/ShopLayoutConfig.asset`); widget recipes (world button / chip / label / sliced sprite) deduplicated into `ShopWorldWidgets`; `ShopChrome.CheckShelfClearance` reads `ShopSectionPanels.FaceBelowHalfHeightFactor` (single source of the 2.5x card-face allowance); absorbed + deleted `ShopChromeConfigSO`.

2026-09-23 config, step 2 — PER-ELEMENT placement (`plans/plan-shop-per-element-placement-2026-09-23.md`; supersedes the step-1 row/stack field model, keeps its mechanics):
- Every element carries its OWN `ElementPlacement { xFromLeftEdge, viewportY, width, height, fontSize }` — 7 chips + exit button as placement structs, ❚❚ right-edge anchored (`optionsRightMargin` + viewportY/size/font), avatar/HP mirror anchors as flat X/Y pairs + shop scales. No stacking arithmetic anywhere; overlaps between elements are NOT guarded (user ruling).
- **Fixed-gap semantics**: X = the element center's world-unit offset from the view's LEFT edge — the band rides the edge, so inter-element gaps stay constant across aspect changes (gap = offset difference − widths, a constant). Y stays viewport (aspect-safe: vertical span locked by orthoSize). Canvas-side X conversion is aspect-free by algebra: `offset x Screen.height / (2 x orthoSize)` (`ShopTopBarLayout.LeftOffsetToCanvasAnchored`); the canvas consumers (CombatIconPresenter / HPNumericDisplayHorizontal, 4 call sites) now pass `MainOrthoSize` (Camera.main, fallback 6.06).
- Defaults are baked from the former row-stack formula at the shipping camera (ortho 6.06, aspect 16:9, 2x halfW = 21.5467) as `...Default` statics on the SO (single default source: field initializers AND code fallbacks); a window at another aspect needs a one-time Play calibration. Target layout for tuning: the UIKitDemo §08 crop (❚❚ above the row line; income on a sub-line under wins/hearts) — reachable purely by editing values.
- Live path unchanged from step 1: SO `OnValidate` → `ShopChrome.ApplyLayout` (build-captured `_rigPos/_halfW/_orthoSize`, scroll-safe) + `ShopPageHud.ApplyMirrorLayout` (`_mirrorDirty` defers mid-travel).

## Shop Top Bar v5 = Authored Prefab (2026-09-24)

`plans/plan-shop-hud-prefab-widgets-2026-09-23.md` — the runtime assembly above is REPLACED by an authored prefab; the step-1/step-2 SO channel for chips/buttons/band is retired (the mirror half of step 2 survives, below).

- **Assets** (`Assets/Prefabs/ShopHud/`): `Tpl_Chip` + `Tpl_WorldButton` base templates; 9 variants (`ChipMoney`, `ChipRarityCommon/Uncommon/Rare`, `ChipWins`, `ChipHearts`, `ChipIncome`, `ExitButton`, `OptionsButton`) carrying key/format/size/font/slot overrides; `ShopHudPage.prefab` nests them at authored positions.
- **Page root components**: `ShopHudPage` (band contract `bandHeight` 2.6 / `bandInsetFromTop` 0.5 / `cameraForwardOffset` 2; `OpenPage(rigPos, orthoSize)` applies the one-shot root placement + captures `BandBottomWorldY`; editor-only gizmo draws the band clearance line) and `ShopHudBinder` (`Collect` wires `HudActionBinding`s; `Refresh` pushes stats incl. Username + the HP pair; `Execute` carries the exit logic incl. the `PhaseTransitionDriver` wrap + legacy fallback).
- **Widget anatomy**: chip = `HudChip` + `HudTextBinding{key, format, target}` on the root, `Panel` (sliced sprite + `PaletteTint` slot) + `Label` (world TMP + `PaletteTint`); button = `BoxCollider2D` + `PhysButton` (serialized refs `_faceRenderer/_shadow/_visualGroup`) + `HudActionBinding`, `Visual/Face` + `Visual/Label` + `Shadow`. `format` supports `{0}/{1}` and TMP rich text (`<size>` mixed sizes = data, not code); labels rewrite only on change (diff guard). `PhysButton` context menu **Sync Collider To Face** re-runs the collider-union math after resizing the Face.
- **Loader**: `ShopChrome.Bootstrap(GameObject hudPagePrefab)` (field on `ShopUXManager`, wired in GameScene) instantiates the page, disables it, places it once, calls `Binder.Collect()`. Public API (`ShowIfActive`/`HideIfActive`/`RefreshIfActive`/`BandBottomWorldY`/`CheckShelfClearance`) unchanged; chromeSprite/chromeFont + `ShopWorldWidgets` remain for `ShopSectionPanels` only.
- **SO shrink**: `ShopLayoutConfigSO` keeps ONLY the 6 mirror fields (superseded the same day — see the mirror section below); `ShopChrome.BandInsetFromTop` resolves page-first (page root field, fallback default).
- **Acceptance**: Play baseline probe JSON (`tools/outputs/shop_hud_baseline_before/after.json`) — 93 fields (positions, sizes, fontSizes, colliders, sorting, label texts) diff EMPTY. Known serialization note: Unity prefab YAML keeps ~7 significant digits; `OptionsButton` label rect (0.72f−0.2f = 0.520000041) needed a one-time full-precision hand patch in the YAML — a future resave of that variant may silently drop the last ulp (1.6e-8 world units, visually nothing; the old runtime path recomputed this value every load).
- User Play matrix (§6.2 of the plan) + free re-layout in the prefab stage pending user verification.

## Avatar/HP Mirror = Prefab Widgets (2026-09-24)

`plans/plan-shop-mirror-prefab-2026-09-24.md` — the 455-line runtime copy machine (`ShopPageHud`) is DELETED; the avatar/HP blocks are authored children of `ShopHudPage.prefab`.

- **Widgets** (`Tpl_Avatar` / `Tpl_HpPill`, nested instances `Avatar` / `HpPill` under the page root): geometry/colors/z-order baked 1:1 from the settled runtime state (probe `tools/outputs/shop_mirror_baseline_before.json`; the copy machine's stale first pair — code-default anchors, pre-login username, wrong canvas unit — was excluded; the prefab system eliminates that whole rebuild-artifact class). Text colors ride `PaletteTint` slots `IconNameLabel` / `HpNormalPlayer`; sprite colors are authored literals copied from the canvas originals.
- **Bindings**: `Tpl_Avatar` root carries `HudTextBinding{Username}` (source `PlayerIdentity.Username`, `"???"` while unnamed); `Tpl_HpPill` root carries `HudCountBinding` (the 0.3 s HP count-up ported from the copy machine: first push snaps, later changes tween, `OnDisable` kills) + `ShopMirrorScale` (the shop scale the canvas pieces glide to — 0.41 / 0.74). `ShopHudBinder.Refresh` pushes Username and `ownerPlayerStatusRef.hp/max(1,hpMax)`.
- **Canvas anchor pairing, world-driven**: `ShopChrome` exposes the build-captured `BuildCameraCenter` + `TryGetAvatarWorldCenter`/`TryGetHpPillWorldCenter`/`AvatarShopScale`/`HpDisplayShopScale`; `ShopTopBarLayout.WorldToCanvasAnchored` inverts a widget's world position into canvas space (X re-expressed via orthoSize × Camera.aspect — the same terms the old left-offset algebra baked). `CombatIconPresenter` + `HPNumericDisplayHorizontal` (park + phase-placement) read `ShopAnchorAvatar`/`ShopAnchorHpDisplay`/`ShopScaleAvatar`/`ShopScaleHpDisplay`; before the chrome is built they fall back to SO-era consts (the deleted asset's user-tuned values). Single placement source: move the widget in the prefab stage, the transition flight follows.
- **Deleted**: `ShopPageHud.cs` (455 lines), `ShopPageHudTests.cs` (goldens tested its dead statics), `ShopLayoutConfigSO.cs` + `ShopLayoutConfig.asset` (the 6 mirror fields were its last content). EditMode baseline 671/670 → **664/663/1 Ignore** (the deleted test file's 7 cases = the plan's expected subtraction).
- **Probe acceptance** (`shop_mirror_baseline_before/after.json`): 85 fields diff NONE. Documented notes: the HpPill ROOT was redefined as the raw canvas-shared anchor (children shifted by the display-root offset so every child's WORLD position is bitwise-identical — verified); the two canvas anchored values differ by the world-inversion float ulp (Δ < 0.001 px).
- User Play matrix (§6.2 of the mirror plan) pending user verification.
